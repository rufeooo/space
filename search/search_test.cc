#define CATCH_CONFIG_MAIN  // Make Catch provide main.

#include <catch2/catch.hpp>

#include "search.h"

TEST_CASE("Path exists using bfs.", "[search - bfs]") {
  REQUIRE(true == false);
}

TEST_CASE("Path exists using dfs.", "[search - dfs]") {
  REQUIRE(true == false);
}

TEST_CASE("Shortest path.", "[search - A*]") {
  REQUIRE(true == false);
}
